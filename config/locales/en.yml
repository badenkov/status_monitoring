en:
  date:
    formats:
      default: "%d.%m.%Y"
  time:
    formats:
      default: "%d.%m.%Y %H:%M"
      short: "%H:%M"
  helpers:
    label:
      monitored_endpoint:
        title: "Title"
        url: "URL"
        threshold: "Threshold, ms"
        interval: "Interval, sec"
    submit:
      monitored_endpoint:
        create: "Create"
        update: "Save"

