<% content_for :title, "New monitored endpoint" %>

<div class="flex justify-between items-center">
  <h1>Add new endpoint</h1>
  <%= link_to "Back", monitored_endpoints_path %>
</div>

<%= render "form", monitored_endpoint: @monitored_endpoint %>

